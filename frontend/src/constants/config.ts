/* eslint-disable @typescript-eslint/no-unsafe-assignment, @typescript-eslint/no-unsafe-member-access */

const config = {
    API_BASE_URL: process.env.API_BASE_URL,
    RESTAURANT_UUID: process.env.RESTAURANT_UUID,
};

export default config;
